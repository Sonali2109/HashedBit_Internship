// Function to get object keys
function getObjectKeys(obj) {
    return Object.keys(obj);
}

// Example usage
const sampleObject = {
    name: "Alice",
    age: 25,
    city: "New York"
};

console.log(getObjectKeys(sampleObject)); // Output: ["name", "age", "city"]
